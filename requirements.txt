# Core deps for cloning and running this public quickjobs tree.
# Create a venv, then: pip install -r requirements.txt && playwright install chromium
curl_cffi>=0.6.0
openpyxl>=3.1.0
playwright>=1.40.0
pypdf>=4.0.0
PyYAML>=6.0
